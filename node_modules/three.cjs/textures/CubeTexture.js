/**
 * @author mrdoob / http://mrdoob.com/
 */
var TextureModule = require('./Texture');
var Three = require('../Three');
var CubeTexture;
CubeTexture = function (images, mapping, wrapS, wrapT, magFilter, minFilter, format, type, anisotropy) {
	mapping = mapping !== undefined ? mapping : Three.CubeReflectionMapping;
	TextureModule.Texture.call(this, images, mapping, wrapS, wrapT, magFilter, minFilter, format, type, anisotropy);
	this.images = images;
};
CubeTexture.prototype = Object.create(TextureModule.Texture.prototype);
CubeTexture.prototype.constructor = CubeTexture;
CubeTexture.clone = function (texture) {
	if (texture === undefined)
		texture = new CubeTexture();
	TextureModule.Texture.prototype.clone.call(this, texture);
	texture.images = this.images;
	return texture;
};
CubeTexture.prototype.isCubeTexture = true;
exports.CubeTexture = CubeTexture;